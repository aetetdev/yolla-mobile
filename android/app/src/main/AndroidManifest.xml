<manifest xmlns:android="http://schemas.android.com/apk/res/android">
    <!-- Keşif haritası kullanıcının konumunu gösteriyor.
         Yalnızca kaba konum yeterli olurdu ama yürüme rotasının başlangıcı
         kullanıcının durduğu nokta olduğu için hassas konum isteniyor.
         İzin verilmezse harita çalışmaya devam eder, sadece kendi konumun
         görünmez — akış izne bağlı değil. -->
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION"/>
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION"/>
    <uses-permission android:name="android.permission.INTERNET"/>

    <application
        android:label="yolla"
        android:name="${applicationName}"
        android:icon="@mipmap/ic_launcher">
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:launchMode="singleTop"
            android:taskAffinity=""
            android:theme="@style/LaunchTheme"
            android:configChanges="orientation|keyboardHidden|keyboard|screenSize|smallestScreenSize|locale|layoutDirection|fontScale|screenLayout|density|uiMode"
            android:hardwareAccelerated="true"
            android:windowSoftInputMode="adjustResize">
            <!-- Specifies an Android theme to apply to this Activity as soon as
                 the Android process has started. This theme is visible to the user
                 while the Flutter UI initializes. After that, this theme continues
                 to determine the Window background behind the Flutter UI. -->
            <meta-data
              android:name="io.flutter.embedding.android.NormalTheme"
              android:resource="@style/NormalTheme"
              />
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>
                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>
        <!-- Don't delete the meta-data below.
             This is used by the Flutter tool to generate GeneratedPluginRegistrant.java -->
        <meta-data
            android:name="flutterEmbedding"
            android:value="2" />

        <!-- Hybrid Composition++ (HCPP).
             MapLibre bir platform görünümü olarak gömülüyor. Varsayılan
             kompozisyon kipinde (TLHC) haritanın **üstüne sonradan eklenen**
             Flutter içeriği ekrana hiç çıkmıyordu: ilk karede var olan
             düğmeler görünürken, bir işarete dokununca açılan kart widget
             ağacında olmasına rağmen çizilmiyordu. HCPP kompozisyonu Android
             görünüm hiyerarşisi düzeyinde yaptığı için bu sınıfı kapatıyor.

             Buradaki eski çözüm `EnableImpeller=false` idi; Flutter 3.44'te
             Android tarafı Impeller-only olduğu için o bayrak artık hiçbir şey
             yapmıyordu — sorunun bir gün çözülmüş görünüp geri gelmesinin
             sebebi buydu.

             Gereksinim: Vulkan + API 34+. Karşılanmazsa motor sessizce
             önceki kipe düşüyor, uygulama çalışmaya devam ediyor. -->
        <meta-data
            android:name="io.flutter.embedding.android.EnableHcpp"
            android:value="true" />
    </application>
    <!-- Required to query activities that can process text, see:
         https://developer.android.com/training/package-visibility and
         https://developer.android.com/reference/android/content/Intent#ACTION_PROCESS_TEXT.

         In particular, this is used by the Flutter engine in io.flutter.plugin.text.ProcessTextPlugin. -->
    <queries>
        <intent>
            <action android:name="android.intent.action.PROCESS_TEXT"/>
            <data android:mimeType="text/plain"/>
        </intent>

        <!-- Rotayı devrettiğimiz navigasyon uygulamaları.
             Android 11'den itibaren paket görünürlüğü kısıtlı: burada şeması
             tanımlı olmayan uygulama KURULU OLSA BİLE canLaunchUrl false döner
             ve kullanıcıya seçenek olarak hiç gösterilmez. -->
        <intent>
            <action android:name="android.intent.action.VIEW"/>
            <data android:scheme="yandexmaps"/>
        </intent>
        <intent>
            <action android:name="android.intent.action.VIEW"/>
            <data android:scheme="yandexnavi"/>
        </intent>

        <!-- Google Haritalar https adresiyle açılıyor; tarayıcı da bir yedek. -->
        <intent>
            <action android:name="android.intent.action.VIEW"/>
            <data android:scheme="https"/>
        </intent>
    </queries>
</manifest>
